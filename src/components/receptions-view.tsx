"use client";
import Link from "next/link";
import { useOpsStore } from "@/components/ops-store";
import { getReceptionDurationMinutes,getRejectionPct,type AcceptanceStatus } from "@/lib/domain";
const timeFmt=new Intl.DateTimeFormat("es-CO",{hour:"2-digit",minute:"2-digit",hour12:false,timeZone:"America/Bogota"});
const statusLabel:Record<AcceptanceStatus,string>={accepted:"Aceptado",conditioned:"Condicionado",partial_rejection:"Rechazo parcial",rejected:"Rechazado",unknown:"Sin dato histórico"};
const statusClass:Record<AcceptanceStatus,string>={accepted:"bg-[var(--green-soft)] text-[var(--green-dark)]",conditioned:"bg-[var(--amber-soft)] text-[var(--amber)]",partial_rejection:"bg-[var(--amber-soft)] text-[var(--amber)]",rejected:"bg-[var(--red-soft)] text-[var(--red)]",unknown:"bg-[var(--surface-soft)] text-[var(--muted)]"};
export function ReceptionsView(){const{receptions}=useOpsStore();const ordered=[...receptions].sort((a,b)=>new Date(b.endedAt).getTime()-new Date(a.endedAt).getTime()),totalKg=ordered.reduce((s,r)=>s+r.netWeightKg,0),known=ordered.filter(r=>r.rejectionKnown!==false),basis=known.reduce((s,r)=>s+r.netWeightKg,0),rejectionKg=known.reduce((s,r)=>s+r.rejectionKg,0),rejectionPct=basis?rejectionKg/basis*100:0,coverage=totalKg?basis/totalKg*100:0;return <><header className="page-header"><div><p className="eyebrow">Operación · entradas</p><h1>Recepciones</h1><p className="lede">Material recibido, inspección, calidad de separación y trazabilidad del lote desde el ingreso.</p></div><div className="header-actions"><Link className="button secondary" href="/">Volver a hoy</Link><Link className="button primary" href="/receptions/new">Nueva recepción</Link></div></header><section className="metrics-grid" aria-label="Indicadores de recepciones"><div className="metric-block"><span>Total registrado</span><strong>{(totalKg/1000).toFixed(2)} t</strong><small>{ordered.length} recepciones</small></div><div className="metric-block"><span>Rechazo cuantificado</span><strong>{rejectionKg.toFixed(0)} kg</strong><small>{rejectionPct.toFixed(1)} % · {coverage.toFixed(0)} % cobertura</small></div><div className="metric-block"><span>Condicionadas/parciales</span><strong>{ordered.filter(r=>r.acceptance==="conditioned"||r.acceptance==="partial_rejection").length}</strong><small>requieren control de calidad</small></div><div className="metric-block"><span>Rechazadas</span><strong>{ordered.filter(r=>r.acceptance==="rejected").length}</strong><small>no crean masa física disponible</small></div></section><section className="panel plant-panel"><div className="section-head"><div><p className="eyebrow">Historial inmediato</p><h2>Ingresos registrados</h2></div><span className="quiet">Más recientes primero</span></div><div className="grid gap-3">{ordered.map(r=><article className="rounded-xl border border-[var(--line)] p-4" key={r.id}><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex flex-wrap items-center gap-2"><strong className="text-sm">{r.lotCode}</strong><span className={`rounded-full px-2 py-1 text-[10px] font-extrabold ${statusClass[r.acceptance]}`}>{statusLabel[r.acceptance]}</span></div><span className="mt-1 block text-xs text-[var(--muted)]">{r.generator} · {r.route} · {r.plant}</span></div><div className="text-right"><strong className="block text-lg">{r.netWeightKg.toLocaleString("es-CO")} kg</strong><span className="text-[11px] text-[var(--muted)]">{r.timePrecision==="date_only"?"Sin hora histórica":timeFmt.format(new Date(r.endedAt))}</span></div></div><div className="mt-4 grid gap-3 border-t border-[var(--line)] pt-3 text-xs sm:grid-cols-4"><div><span className="quiet">Residuo</span><strong className="mt-1 block">{r.wasteType}</strong></div><div><span className="quiet">Rechazo</span><strong className="mt-1 block">{r.rejectionKnown===false?"Sin dato cuantitativo":`${r.rejectionKg} kg · ${getRejectionPct(r).toFixed(1)} %`}</strong></div><div><span className="quiet">Duración</span><strong className="mt-1 block">{r.source==="historical"?"No disponible":`${Math.round(getReceptionDurationMinutes(r))} min`}</strong></div><div><span className="quiet">Origen dato</span><strong className="mt-1 block">{r.source==="demo"?"Demo inicial":r.source==="historical"?"Importación histórica":"Registrado en app"}</strong></div></div>{r.observation&&<p className="mt-3 rounded-lg bg-[var(--surface-soft)] p-3 text-xs text-[var(--muted)]">{r.observation}</p>}</article>)}</div></section></>}

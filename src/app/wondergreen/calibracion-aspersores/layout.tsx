import type { Metadata } from "next";

export const metadata: Metadata = { title: "Calibración de aspersores", description: "Organiza tanque, cobertura y mediciones de aplicación para revisar la uniformidad antes de usar producto.", alternates: { canonical: "/wondergreen/calibracion-aspersores/" } };
export default function CalibracionAspersoresLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }

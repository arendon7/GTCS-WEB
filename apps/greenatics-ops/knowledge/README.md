# Knowledge System

`memory.jsonl` es canónico y append-only. `obsidian-vault/` sirve para navegación humana. `graph/` es derivado y puede regenerarse.

No editar decisiones antiguas para actualizarlas: crear una nueva entrada con `supersedes`.
